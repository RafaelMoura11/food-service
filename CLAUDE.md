## Agent skills

### Issue tracker

Issues live in GitHub Issues (RafaelMoura11/food-service), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
